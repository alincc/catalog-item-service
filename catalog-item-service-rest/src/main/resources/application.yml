info:
  app:
    name: catalog-item-service
    description: Finds metadata and content links about a item

server:
  port: 0

ni:
  servers:
    - localhost:7654

zipkin:
    collector-hostname: localhost