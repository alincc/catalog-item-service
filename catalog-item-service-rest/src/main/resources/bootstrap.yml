spring:
  application:
    name: catalog-item-service
  cloud:
    config:
      failFast: false
      discovery:
        enabled: true

server:
  port: 0


---

spring:
  profiles: docker
  cloud:
    config:
      failFast: true

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/