spring:
  application:
    name: catalog-item-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      failFast: false

---

spring:
  profiles: docker
  cloud:
    config:
      failFast: true